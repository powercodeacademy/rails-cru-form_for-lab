<h1>Create Artists</h1>
<%= form_for(@artist) do |f| %>
  <%= f.label :name %><br>
  <%= f.text_field :name %><br>

  <%= f.label :bio %><br>
  <%= f.text_field :bio %><br>

  <%= f.submit 'Update Artist' %>
<% end %>
