<h1>Artists</h1>

<ul>
  <% @artists.each do |artist| %>
    <li>
      <h4><%= link_to artist.name, article %></h4>
      <p> <%= artist.bio %></p>
    </li>
  <% end %>
</ul>

<%= link_to "New Artist", new_artist_path %>
