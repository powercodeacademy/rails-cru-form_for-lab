<%= link_to "My Song", new_song_path %>

<div>
  <h1> @songs.each do |song| </h1>
    <p>Artist: <%= link_to @song.artist.name, @song.artist %></p>
    <p>Genre: <%= link_to @song.genre.name, @song.genre %></p>
  <% end %>
</div>
